// Site layout for non-home pages (about, artists)
// All CSS is now centralized in globals.css

export default function SiteLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}

