// Home-only CSS: welcome intro animation styles
import "../../styles/legacy/welcomeAnim.css";

export default function HomeLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}

